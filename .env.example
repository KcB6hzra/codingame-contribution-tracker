# CodinGame Session Cookie
# You can find this in your browser's dev tools after logging in to CodinGame
CG_COOKIE="rememberMe=...;"

# CodinGame User ID
CG_USER_ID=1234567

# Extra handles to track (e.g. private contributions not in pending/accepted lists)
EXTRA_HANDLES=handle1,handle2

# Specific handles to track for testing (comma-separated list)
# If set, only these contributions will be processed.
TEST_HANDLES=handle1,handle2,handle3
